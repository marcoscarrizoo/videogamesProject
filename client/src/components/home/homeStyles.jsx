import { makeStyles } from "@material-ui/core";

export default makeStyles({
  container1: {
    display: "grid",
    gridTemplateColumns: "repeat(4, 1fr)",
  },
  root: {
    background: "#708090",
  },
  loading: {
    
  }
});
